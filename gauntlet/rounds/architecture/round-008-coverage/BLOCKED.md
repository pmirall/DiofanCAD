# Round 008 — BLOCKED

This round's bar was written and the build was made, but PartDesign does not
work in the console configuration: 0 of 20 tests pass, 16 throwing a non-std
exception when creating a `PartDesign::Body` and 4 segfaulting on saved models.

The bar in `bar.md` stands. The round is deferred until PartDesign runs, and
the cause of that is itself unmeasured — see `CLAUDE.md` §2.

Round 009 was taken instead, because it needs no build at all.
